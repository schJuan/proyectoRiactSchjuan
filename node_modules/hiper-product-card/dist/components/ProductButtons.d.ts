import { CSSProperties } from "react";
export interface Props {
    className?: string;
    style?: CSSProperties;
}
export declare const ProductButtons: ({ className, style }: Props) => JSX.Element;
