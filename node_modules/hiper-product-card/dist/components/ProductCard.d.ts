import React from "react";
import { Product, ProductContextProps, onChangeArgs, InitalValues, ProductHandlers } from "../interfaces/interfaces";
import { CSSProperties } from "react";
export interface Props {
    className?: string;
    product: Product;
    children: (args: ProductHandlers) => JSX.Element;
    style?: CSSProperties;
    value?: number;
    onChange?: (args: onChangeArgs) => void;
    initialValues?: InitalValues;
}
export declare const ProductContext: React.Context<ProductContextProps>;
export declare const ProductCard: ({ children, product, value, className, style, onChange, initialValues }: Props) => JSX.Element;
